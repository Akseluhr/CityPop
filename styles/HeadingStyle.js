import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    mainHeader: {
      textAlign: "center",
      fontWeight: "bold",
      fontSize: 30,
      top: -150
    },
  });
export default styles